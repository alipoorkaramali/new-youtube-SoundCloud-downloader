name: 🔐🔎Check RSS Log and Trigger Downloads (DISABLED - Railway is handling it)

# =============================================
# این workflow کاملاً غیرفعال شده چون Railway Worker فعال است
# =============================================

# on:
#   schedule:
#     - cron: '*/10 * * * *'
#   workflow_dispatch:

permissions:
  contents: write
  actions: write

jobs:
  check:
    runs-on: ubuntu-latest
    if: false   # ← این خط باعث میشه workflow هیچ‌وقت اجرا نشه
    steps:
      - name: This workflow is disabled
        run: echo "Workflow disabled on railway-test branch - Railway is the main executor now"
